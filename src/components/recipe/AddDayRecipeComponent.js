import React from 'react';
import { FiPlusCircle } from "react-icons/fi";

const AddDayRecipe = ({ onClick }) => {

    return (
        <div className="addDayRecipeContainer">

            <FiPlusCircle className="addIcon"/>

        </div>
    );
};

export default AddDayRecipe;
